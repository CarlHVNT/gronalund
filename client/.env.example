# Leave unset for local dev (Vite proxies /api to http://localhost:4000, see vite.config.js).
# In production (Netlify), set this to the deployed server's origin, e.g.:
# VITE_API_URL=https://gronalund-server.onrender.com
VITE_API_URL=
